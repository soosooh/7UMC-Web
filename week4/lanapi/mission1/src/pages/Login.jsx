import styled from 'styled-components';

const Login = () => {
    return (
        <WhiteHeading> 로그인 페이지</WhiteHeading>
    );
};

export default Login;

const WhiteHeading = styled.h1`
    color: white;
`